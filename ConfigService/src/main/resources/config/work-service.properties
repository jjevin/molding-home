spring.datasource.platform=postgres

spring.jpa.hibernate.ddl-auto=none
spring.jpa.database=POSTGRESQL
spring.datasource.platform=postgres
spring.jpa.show-sql = true
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.database.driverClassName= org.postgresql.Driver
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

spring.cloud.loadbalancer.ribbon.enabled = false
spring.cloud.gateway.enabled = true
spring.cloud.gateway.lowerCaseServiceId = true

management.endpoints.web.exposure.include=*
management.endpoints.enabled-by-default=true
management.endpoint.health.enabled = true
management.endpoint.health.show-details = always

eureka.instance.preferIpAddress = true
eureka.client.registerWithEureka = true
eureka.client.fetchRegistry = true
eureka.client.serviceUrl.defaultZone = http://eurekaserver:8070/eureka/

# keycloak.realm = molding
# keycloak.auth-server-url = http://keycloak:8080
# keycloak.ssl-required = external
# keycloak.resource = molding
# keycloak.credentials.secret = Vvwte4acDtYxkxp2gAKjr9zfD7maaXsj
# keycloak.use-resource-role-mappings = true
# keycloak.bearer-only = true

spring.security.oauth2.resourceserver.jwt.issuer-uri = http://keycloak:8080/realms/molding
